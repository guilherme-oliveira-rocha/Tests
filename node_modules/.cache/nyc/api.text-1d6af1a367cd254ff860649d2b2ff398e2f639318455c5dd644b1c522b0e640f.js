const { describe, it, after, before } = require('mocha');
const supertest = require("supertest");
const assert = require("assert");
describe("Api Suite test", () => {

  let app;

  before((done) => {
    app = require('./api');
    app.once('listening', done);
  });

  after(done => app.close(done));

  describe("/contact:get", () => {
    it(
      "should request the contact route and return HTTP Status 200",
      async () => {
        const response = await supertest(app).get("/contact").expect(200);

        assert.strictEqual(response.text, "contact us page");
      }
    );
  });

  describe('/login:post', () => {
    it(
      'should request the login route and return HTTP Status 200',
      async () => {
        const response = await (await supertest(app).post('/login')).setEncoding({username:"Guilherme", password: "123"})(200);

        console.log("response);
        assert.strictEqual(response.text, 'Log in succeeded!');
      }
    );
  });
});
